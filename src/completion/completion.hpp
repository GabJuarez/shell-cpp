#pragma once

namespace completion {
    // Initialize Readline autocompletion for built-in commands
    // Call this once during shell setup to enable completing built-in names
    void initialize();
}
